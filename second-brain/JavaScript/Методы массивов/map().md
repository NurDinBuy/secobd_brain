Метод создаёт новый массив с результатом вызова указанной функции для каждого элемента массива.


Пример:
```
const numbers = [1, 4, 9];
const doubles = numbers.map((num) => num * 2);
```

[[Методы массивов]]